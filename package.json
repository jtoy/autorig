{
  "name": "distark-render",
  "version": "1.0.0",
  "description": "Rendering-agnostic character rig system with TypeScript",
  "type": "module",
  "main": "./dist/renderRig.js",
  "types": "./dist/renderRig.d.ts",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "dev": "tsc --watch & http-server -p 8080",
    "clean": "rm -rf dist",
    "serve": "http-server -p 8080"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
